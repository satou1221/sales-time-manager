# Sales Time Manager 作業説明書

## 1. はじめに
「Sales Time Manager」は、営業部の業務時間を簡単かつ正確に記録・管理するためのスマートフォン向けWebアプリケーションです。
日々の業務区分をタップするだけで、通常業務・時間外業務・休憩などを自動的に計算し、月末にはLINE WORKS経由で簡単に報告データを提出することができます。PCブラウザでアクセスした際は、画面中央にスマートフォンサイズのアプリが表示され、見やすいレイアウトで利用できます。

## 2. 初期設定とアプリのインストール

### 2.1. アプリへのアクセス
スマートフォン（iPhoneまたはAndroid）のブラウザ（SafariまたはChrome）を開き、管理者から共有されたURLにアクセスしてください。

### 2.2. 初期設定（初回のみ）
初めてアクセスした際、初期設定画面が表示されます。以下の項目を正確に入力してください。
- **社員番号**: 6桁の数字で入力してください。
- **氏名**: 姓と名の間にスペースを入れずに入力してください（例：山田太郎）。
- **部門**: プルダウンから「営業部本社」または「営業部福岡支社」を選択してください。
- **就業時間**: 始業時刻と終業時刻を設定します（デフォルトは08:30〜17:30）。
- **休憩時間**: 休憩の開始時刻と終了時刻を設定します（デフォルトは12:00〜13:00）。

入力後、「設定を保存して開始」をタップするとホーム画面に移動します。

### 2.3. ホーム画面への追加（アプリ化）
毎回ブラウザからアクセスする手間を省くため、スマートフォンのホーム画面に追加（インストール）することを強く推奨します。
1. 画面下部の「設定」タブをタップします。
2. 「アプリ化（ホーム画面に追加）の方法を表示」ボタンをタップします。
3. 画面の指示に従って、iPhone（Safari）またはAndroid（Chrome）の機能を使ってホーム画面に追加してください。
4. 以降は、ホーム画面に追加されたアイコンからアプリを起動してください。

## 3. 日々の業務記録（ホーム画面）

ホーム画面では、現在の業務状態の確認と、業務区分の切り替えを行います。

### 3.1. 業務の開始と切り替え
画面に並んでいる業務区分ボタン（「九電碍・点」「九電管路」など）をタップすると、その業務の計測が開始されます。
- **自動切り替え**: 別の業務区分ボタンをタップすると、現在進行中の業務が自動的に終了し、新しい業務の計測が開始されます。終了ボタンを都度押す必要はありません。
- **選択中の強調**: 現在計測中の業務ボタンは緑色に変わり、右上に「選択中」バッジが表示されます。

### 3.2. 休憩と懇親会
- **休憩開始**: 「休憩開始」ボタンをタップすると休憩時間の計測が始まります。休憩から業務に戻る際は、再開する業務区分のボタンをタップしてください。
- **懇親会対応**: 「懇親会対応を開始」ボタンをタップすると、懇親会対応時間として計測されます。

### 3.3. 業務の終了
一日の業務がすべて終わったら、画面下部の赤い「業務終了」ボタンをタップしてください。計測が停止し、「待機中」状態に戻ります。

### 3.4. 時間外業務の自動判定
就業時間（例：08:30〜17:30）の前後に業務を行った場合、システムが自動的に「時間外業務」として計算・記録します。手動で時間外を指定する必要はありません。
また、業務中に始業時刻や終業時刻を跨いだ場合も、システムが自動的に履歴を分割して正しく計算します。アプリを閉じていた間に始業・終業時刻を跨いだ場合でも、次にアプリを開いた際や業務終了時に、適切な区分（通常/時間外）に分割して保存されます。

### 3.5. 押し忘れ防止機能
記録漏れを防ぐため、以下の警告機能が備わっています。
- **待機中バッジの点滅**: 業務未開始の時は「待機中」バッジが点滅して知らせます。
- **業務未開始警告**: 就業時間内に業務を開始しないまま10分経過すると、画面上部に警告バナーが表示されます。
- **休憩未開始警告**: 設定した休憩時間内に休憩を開始しないまま10分経過すると、警告バナーが表示されます。
- **長時間業務警告**: 同じ業務区分を90分以上継続していると、確認を促す警告バナーが表示されます。
- **終業後の継続確認**: 終業時刻から20分経過すると、時間外計測を続行するか確認するダイアログが表示されます。

## 4. 履歴の確認と修正（本日ページ）

画面下部の「本日」タブをタップすると、今日の業務履歴と集計を確認できます。

### 4.1. 本日の集計
画面上部に、本日の「通常業務」「時間外」「休憩」「懇親会」「休暇中業務」の合計時間が表示されます。また、業務時間の内訳を視覚的に把握できるよう、円グラフで各業務区分の時間とパーセンテージが表示されます。

### 4.2. 履歴の修正とメモの追加
記録された履歴リストの各項目をタップすると、修正画面が開きます。
- **時間の修正**: 開始時刻や終了時刻を修正できます。
- **メモの追加**: 業務内容に関するメモを入力できます。
- **削除**: 誤って記録した履歴は「この記録を削除」ボタンで消去できます。

## 5. 休暇・休日の設定（設定ページ）

「設定」タブでは、有給休暇や時間休、休日の設定を行います。

### 5.1. カレンダーによる休日・有給設定
カレンダーの日付をタップするごとに、以下の順で状態が切り替わります。
1. **通常**（色なし）
2. **休日**（赤色）
3. **有給**（緑色）

※土日および祝日は自動的に休日として扱われます。

### 5.2. 時間休の設定
時間休を取得する場合は、カレンダー下の「時間休設定」フォームを使用します。
1. 日付を選択します。
2. 時間休の開始時刻と終了時刻を入力します。
3. 「時間休を保存」をタップします。
設定が完了すると、カレンダー上の該当する日付が青色（時間休）で表示されます。

### 5.3. 休暇中の業務記録
有給休暇や休日、時間休の期間中に業務を行った場合、ホーム画面で業務区分をタップして記録すると、自動的に「休暇中業務」として集計されます。

## 6. 月次報告とCSV出力（月次ページ）

月末には、「月次」タブから業務時間の集計データを出力し、管理者に報告します。

### 6.1. 月次集計の確認
画面上部で確認したい年月を選択すると、その月の総業務時間や各区分の合計時間、および円グラフが表示されます。円グラフには各業務区分の時間とパーセンテージが表示され、月間の業務配分を一目で確認できます。
日別のリストをタップすると、その日の詳細な履歴を確認できます。

### 6.2. CSVデータの出力と報告
1. 画面上部の「CSV出力（LINE WORKS共有）」ボタンをタップします。
2. スマートフォンの共有メニュー（シェアシート）が開きます。
3. アプリ一覧から「LINE WORKS」を選択し、指定された管理者または報告用トークルーム宛に送信してください。
※共有メニューが開かない機種の場合は、自動的にCSVファイルがダウンロードされますので、そのファイルをLINE WORKS等で送信してください。

## 7. 注意事項
- 本アプリのデータはスマートフォンの内部（ブラウザの保存領域）に保存されます。**ブラウザのキャッシュや履歴を完全に削除すると、記録データも消去される**恐れがありますのでご注意ください。
- 毎月のCSV出力・報告が完了するまでは、データを保持しておくことをお勧めします。
