# Sales Time Manager アプリ v1.65 アップデート完了報告

## 概要

Sales Time Manager アプリのバージョン 1.65 がリリースされました。今回のアップデートでは、主に以下の改善が行われています。

- **休暇中業務の判定ロジック修正**: 時間休取得時の業務記録が正しく処理されるようになりました。
- **キャッシュ回避策の導入**: アプリの更新がユーザーに確実に反映されるよう、キャッシュ戦略を改善しました。

## 修正内容の詳細

### 1. 休暇中業務の判定ロジック修正

これまで、時間休を取得している場合に、時間休中の業務が時間外業務として誤って計上される不具合がありました。v1.65 では、`isHolidayOrSpecial` 関数および `isCurrentOT` 関数、`calculateRecordMinutes` 関数、`checkWorkSplit` 関数を修正し、時間休中の業務が適切に処理されるように改善しました。

具体的には、時間休設定日には時間外業務の判定をスキップし、時間休中の業務は「休暇中業務」として正しく計上されるようになりました。

### 2. キャッシュ回避策の導入

アプリのアップデート時に古いバージョンのファイルがブラウザにキャッシュされ、最新版が適用されない問題に対応するため、以下のキャッシュ回避策を導入しました。

- `app.js` 内の `APP_VERSION` を `v1.65` に更新しました。
- `index.html` 内で読み込む `app.js` および `style.css` のファイルパスにバージョンクエリパラメータ (`?v=1.65`) を追加しました。
- Service Worker (`sw.js`) のキャッシュ名を `sales-time-v72` に更新し、キャッシュ対象ファイルにもバージョンクエリパラメータを追加しました。

これにより、ユーザーがアプリにアクセスした際に、常に最新バージョンのファイルが読み込まれるようになります。万が一、古いバージョンが表示される場合は、アプリ内の「アプリを強制更新する」ボタンをクリックするか、ブラウザのキャッシュをクリアしてください。

## ユーザーへのご案内

今回のアップデートにより、時間休取得時の業務記録がより正確になりました。引き続き、Sales Time Manager アプリをご活用ください。

ご不明な点がございましたら、お気軽にお問い合わせください。

--- 

**Manus AI** より
